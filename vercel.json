{
  "_comment": "SHARED CONFIG: Both projects use this file. Environment detection in trigger-daily-emails.js prevents dev from sending emails",
  "crons": [
    {
      "path": "/api/trigger-daily-emails",
      "schedule": "0 11 * * *"
    }
  ],
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}